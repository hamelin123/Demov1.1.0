import React from 'react';

interface ShipmentDetailsLayoutProps {
  children: React.ReactNode;
}

export default function ShipmentDetailsLayout({ children }: ShipmentDetailsLayoutProps) {
  return <>{children}</>;
}
