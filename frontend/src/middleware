import createMiddleware from 'next-intl/middleware';

export default createMiddleware({
  // ภาษาที่รองรับ
  locales: ['en', 'th'],
  
  // ภาษาเริ่มต้น
  defaultLocale: 'en'
});

export const config = {
  // รูปแบบ path ที่ต้องผ่าน middleware นี้
  matcher: ['/((?!api|_next|.*\\..*).*)']
};